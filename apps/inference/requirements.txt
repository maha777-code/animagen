# Animagen Inference Worker (Phase 5)
#
# Self-hosted vLLM worker for Tier 2 SceneSpec generation.
# Consumes jobs from Redis Stream / BullMQ.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
redis>=5.1.0

# vLLM installed separately on GPU nodes:
# pip install vllm>=0.6.0
