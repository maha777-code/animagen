# Infrastructure (Phase 6)

Placeholder for:
- `docker/` — Dockerfiles for web, api, inference
- `docker-compose.dev.yml` — local dev stack
- `k8s/` — production manifests with HPA
- `nginx/` — CDN cache config for SceneSpec responses
- `k6/` — load test script (10k RPS, 95% cache hit)
